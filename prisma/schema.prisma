// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// À compléter avec les modèles pour Smart-Collect
// Exemples:
// - User (admins, techniciens, superviseurs)
// - Device (capteurs IoT ESP32)
// - Collection (historique des collectes)
// - Alert (alertes de débordement)
// - Route (itinéraires de collecte)
